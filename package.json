{
  "name": "purmemo-amp",
  "version": "0.0.0",
  "private": true,
  "description": "AMP — AI Memory Protocol. The open protocol for AI conversation portability.",
  "scripts": {
    "build": "pnpm -r run build",
    "test": "pnpm -r run test",
    "lint": "pnpm -r run lint",
    "publish-all": "pnpm -r publish --access public"
  },
  "devDependencies": {
    "@changesets/cli": "^2.27.0",
    "typescript": "^5.4.0",
    "tsup": "^8.0.0",
    "vitest": "^1.6.0"
  },
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=9.0.0"
  }
}
